# Quarantined 1st-order accel stack (multi-shard host-reduces the cavity source).
# Not the chimera production multi-GPU path. See src/legacy/accel_solver_1st_order.jl
error("src/accel_solver_1st_order.jl is quarantined. Production 2nd-order multi-GPU is chimera/mgpu (NCCL Allreduce). The old accel file is in src/legacy/accel_solver_1st_order.jl")
